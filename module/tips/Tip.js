var Tip = (function (window, undefined) {
    var Tip = {
        html: '<div class="tip"><span class="tip-info"></span>' +
                '<span class="tip-ack"></span>' +
                '<span class="tip-close">X</span>' +
              '</div>',
        infoText: '好消息',
        ackText: '我知道了'

    };

    return Tip;
}(window));